Verify an Opportunity Map change against the browser-first contract. Optional focus: $ARGUMENTS

Open and follow `.cursor/skills/map-contract-verify/SKILL.md`. Path list authority is `scripts/ma-skill-registry.json`.

Offline skill-layer check (no Map drive):

```bash
npm run verify:ma-skills
```

Do not invent a second browser framework. Do not treat unit tests alone as Map verification.
