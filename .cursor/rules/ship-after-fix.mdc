---
description: Always commit and deploy after completing a fix or feature in market-assassin
alwaysApply: true
---

# Ship After Fix (Eric preference)

When you finish a bug fix or feature in **market-assassin**:

1. **Commit + push** before claiming done — do not leave fixes local-only.
2. **Deploy** with `vercel --prod` from the **same worktree/branch** that has the fix (after push succeeds).
3. **Verify** prod by grepping the **specific string/element you changed** on the live URL — not a 200, not a green build status.
4. Before any `vercel --prod` from a linked worktree, confirm **that worktree** has its own `.vercel/project.json`. See below.

Do not ask "want me to commit/deploy?" — just do it unless the change is explicitly exploratory/draft.

Commit messages end with: `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`

Feature pushes: append proof to `docs/MARKETING-FEATURE-LITERATURE.md` in the same commit.

## Worktree isolation (never mix with main)

- Do **not** commit or deploy from a checkout that is on `main`, and do **not** merge agent work into `main` unless Eric explicitly asks.
- Prefer a dedicated **git worktree** on a feature/chore branch at `.claude/worktrees/<slug>`. Keep Eric's primary Desktop checkout free of agent branch switches.
- If the primary repo is already on a safe non-`main` branch and Eric is using it, you may work there — but never `git checkout main`, never `git merge main` into the agent branch unless asked, and never `git push origin main`.
- Push only `git push -u origin HEAD` for the current feature branch.
- When creating a new worktree, fetch first and pin the start ref to an explicit remote branch. Never start from implicit HEAD or the local `main` checkout:
  `git fetch origin && git worktree add -b <branch> .claude/worktrees/<slug> origin/<base>`
  `<base>` is usually `main`.
- To continue an existing remote branch, fetch first and create a corresponding local branch:
  `git fetch origin && git worktree add -b <local-branch> .claude/worktrees/<slug> origin/<remote-branch>`
  If the local branch already exists and is not checked out elsewhere:
  `git worktree add .claude/worktrees/<slug> <local-branch>`
  Never continue feature work from a detached HEAD.
- Do all edits, commits, and deploys from that worktree path.
- After creating the worktree, also run `npm run env:link-worktree -- .claude/worktrees/<slug>` and copy the main checkout's `.vercel/` into the worktree (or `vercel link --yes --project market-assassin` from the worktree). A worktree without its own `.vercel/project.json` is not deploy-ready.

## ⚠️ Vercel CLI from a worktree — link `.vercel` locally or you ship the parent checkout

`vercel --prod` uploads the **directory that owns the nearest `.vercel/project.json`**, not "whatever branch your shell thinks it is on."

A fresh git worktree under `.claude/worktrees/<slug>` does **not** inherit the main checkout's `.vercel/`. If that file is missing, the CLI walks up to the parent `market-assassin` checkout, uploads **that** tree (often still on `main` / an older branch), aliases production, and reports READY — while the feature branch you just pushed never reached the build. Measured 2026-09-14 on PR #1514: two "successful" prod deploys served the old Confirm UI; only after copying `.vercel/` into the worktree and redeploying did `Edit question` / Ask-collapse ship.

**Before every worktree `vercel --prod`:**

```bash
test -f .vercel/project.json || {
  echo "REFUSED: this worktree has no .vercel link — copy main's .vercel/ or run vercel link"
  exit 1
}
# cwd must be the worktree that contains the fix
pwd   # must end with .claude/worktrees/<slug>
```

Do **not** treat a green Vercel READY as proof the branch shipped. Grep the live chunk/page for a string that exists only on the feature branch.
